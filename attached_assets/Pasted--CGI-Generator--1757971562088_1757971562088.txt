# 🎬 برومبت شامل لبناء CGI Generator من الصفر على ريبليت

## 📝 **البرومبت الكامل (انسخه ده كله في ريبليت):**

---

**يا جدع انا عايز اعمل مشروع SaaS اسمه "CGI Generator" على ريبليت من الصفر خالص. المشروع ده عايز يبقى منصة كاملة المستخدمين يرفعوا عليها صورة منتج وصورة مشهد والذكاء الاصطناعي يعملهم صورة او فيديو CGI احترافي يدمج المنتج في المشهد بواقعية.**

**المطلوب بالتفصيل:**

## 🏗️ **1. البنية التقنية:**

**Backend:**
- Node.js + Express server
- نظام Authentication بـ JWT
- نظام Credits للمستخدمين (كل مستخدم جديد ياخد 5 كريدت مجانا)
- صورة CGI = 1 كريدت، فيديو CGI = 5 كريدت
- رفع الصور بـ Multer وحفظها على Cloudinary
- معالجة الـ CGI في 4 مراحل:
  1. تحسين الوصف بـ Gemini AI ($0.001)
  2. إنتاج صورة CGI بـ Fal.ai ($0.05) 
  3. برومبت للفيديو بـ Gemini ($0.003)
  4. إنتاج فيديو CGI بـ Fal.ai ($0.50)

**Frontend:**
- HTML + Tailwind CSS + Vanilla JavaScript
- صفحات: index.html, dashboard.html, pricing.html
- واجهة عربية من اليمين لليسار (RTL)
- تصميم responsive يشتغل على الموبايل
- نظام drag & drop لرفع الصور
- progress bars للمتابعة في real-time

## 🔑 **2. الـ APIs المطلوبة:**

```javascript
// المفاتيح دي استخدمها في Environment Variables
GEMINI_API_KEY = "AIzaSyDL1FDQJy63SjCC4sP87kvQ6B-ht_enl-8"
FAL_API_KEY = "2fd637d9-6952-44a0-a5c2-1ab874fd3fa9:8924390041c47891bb58f6260270ad6e"
CLOUDINARY_CLOUD_NAME = "dqtsski2w"
CLOUDINARY_API_KEY = "882716225516264"
CLOUDINARY_API_SECRET = "k18dJOendyCp95RzyFhLAxbQW2A"
STRIPE_SECRET_KEY = "sk_test_..." // للمدفوعات لاحقا
JWT_SECRET = "cgi-generator-secret-2024"
```

## 📱 **3. صفحات الموقع المطلوبة:**

### **صفحة index.html:**
- Hero section جذاب بعنوان "اصنع صور وفيديوهات CGI احترافية بالذكاء الاصطناعي"
- شرح كيفية العمل في 3 خطوات
- أزرار تسجيل دخول وإنشاء حساب
- أمثلة على النتائج (قبل وبعد)

### **صفحة dashboard.html:**
- رفع الصور بـ drag & drop
- معاينة الصور قبل الرفع
- اختيار نوع المحتوى (صورة/فيديو)
- وصف اختياري للمشروع
- عرض رصيد الكريدت الحالي
- قائمة المشاريع السابقة مع حالة كل مشروع
- progress bar مفصل لكل مرحلة

### **صفحة pricing.html:**
- 3 باقات: المبتدئ (10 كريدت - $9.99), المحترف (50 كريدت - $39.99), المؤسسات (200 كريدت - $149.99)
- جدول مقارنة المميزات
- تكامل مع Stripe للمدفوعات

## 🔄 **4. مراحل معالجة المحتوى:**

### **المرحلة 1: تحسين الوصف**
```javascript
// استدعي Gemini AI عشان يحسن الوصف ويخليه احترافي
const enhancedPrompt = await callGeminiAPI(`
انت خبير في CGI والتصوير التجاري. حلل الصورتين دول واعمل وصف احترافي:
- صورة المنتج: ${productImageUrl}
- صورة المشهد: ${sceneImageUrl}  
- طلب المستخدم: ${userDescription}

اعمل prompt احترافي يتضمن:
- وصف دقيق للمنتج والمواد اللي هو مصنوع منها
- تحليل الإضاءة والظلال في المشهد
- موقع مناسب للمنتج في المشهد
- مطابقة الألوان والإضاءة
- مواصفات تقنية للجودة العالية

اكتب الوصف بالإنجليزية للـ AI image generator
`);
```

### **المرحلة 2: إنتاج الصورة**
```javascript
// استدعي Fal.ai عشان ينتج صورة CGI
const imageResult = await callFalAI({
  model: "fal-ai/flux-pro",
  prompt: enhancedPrompt,
  image_url: sceneImageUrl,
  mask_image_url: productImageUrl,
  width: 1024,
  height: 1024,
  num_inference_steps: 50,
  guidance_scale: 7.5
});
```

### **المرحلة 3: برومبت الفيديو**
```javascript
// استدعي Gemini تاني عشان يعمل prompt للفيديو
const videoPrompt = await callGeminiAPI(`
بناء على الصورة المنتجة دي: ${imageResult.url}
اعمل prompt لإنتاج فيديو 5 ثوان يظهر المنتج بحركة طبيعية
`);
```

### **المرحلة 4: إنتاج الفيديو**
```javascript
// استدعي Fal.ai تاني للفيديو
const videoResult = await callFalAI({
  model: "fal-ai/stable-video-diffusion",
  image_url: imageResult.url,
  prompt: videoPrompt,
  video_length: 5,
  fps: 24
});
```

## 💾 **5. نظام قاعدة البيانات:**

**ابدأ بـ in-memory arrays وبعدين نحولها MongoDB:**
```javascript
let users = []; // {id, name, email, password, credits, createdAt}
let jobs = [];   // {id, userId, title, status, progress, outputUrl, createdAt}
let transactions = []; // {id, userId, amount, credits, status, createdAt}
```

## 🔐 **6. نظام الأمان:**

- JWT tokens للمصادقة
- bcrypt لتشفير كلمات المرور  
- rate limiting للـ APIs
- validation للصور المرفوعة
- CORS headers صحيحة

## 🎨 **7. التصميم والـ UI:**

- استخدم Tailwind CSS
- خط Cairo للعربية
- ألوان: بنفسجي gradients (#667eea إلى #764ba2)
- أيكونات جميلة وemojis
- hover effects ونيم animations
- toast notifications للرسائل

## 📊 **8. المتابعة والتحليل:**

- progress tracking في real-time
- error handling محترف
- logging مفصل لكل عملية
- إحصائيات للمستخدم (عدد المشاريع، معدل النجاح)
- system health checks

## 💳 **9. نظام المدفوعات:**

- تكامل مع Stripe
- 3 باقات مختلفة
- نظام webhooks للتأكيد الآمن
- تاريخ المدفوعات
- refund للكريدت في حالة فشل المعالجة

## 🚀 **10. الـ Endpoints المطلوبة:**

```
POST /api/register - إنشاء حساب جديد
POST /api/login - تسجيل دخول  
GET /api/profile - بيانات المستخدم
POST /api/upload-images - رفع الصور
POST /api/create-cgi-job - بدء مشروع CGI
GET /api/jobs - قائمة مشاريع المستخدم
GET /api/jobs/:id/status - متابعة حالة المشروع
GET /api/jobs/:id/download - تحميل النتيجة
POST /api/buy-credits - شراء كريدت
GET /api/pricing - باقات الأسعار
```

## 📁 **11. هيكل الملفات المطلوب:**

```
/
├── server.js (الـ backend الرئيسي)
├── package.json (dependencies)
├── public/
│   ├── index.html (الصفحة الرئيسية)
│   ├── dashboard.html (لوحة التحكم)  
│   ├── pricing.html (باقات الأسعار)
│   └── style.css (التصميمات الإضافية)
├── .env (متغيرات البيئة)
└── README.md (شرح المشروع)
```

## 🎯 **12. المطلوب منك:**

1. **اعمل replit جديد** باسم "cgi-generator"
2. **ابني الهيكل كله** بالملفات والكود
3. **اعمل الـ frontend** بالعربية وتصميم جميل
4. **اعمل الـ backend** بكل الـ endpoints
5. **ادمج الـ APIs** (Gemini, Fal.ai, Cloudinary)
6. **اختبر كل حاجة** واتأكد إنها شغالة
7. **اعمل error handling** محترف
8. **اضيف logging** مفصل لكل عملية

## ⚡ **13. نصائح مهمة:**

- استخدم async/await في كل حتة
- اعمل try/catch لكل API call
- اضيف progress bars جميلة
- اعمل الواجهة responsive
- استخدم localStorage لحفظ الـ tokens
- اعمل toast notifications للرسائل
- اضيف loading states في كل مكان

## 🔥 **14. الهدف النهائي:**

**عايز منصة CGI Generator شغالة 100% على ريبليت، المستخدم يقدر:**
- ينشئ حساب ويخش
- يرفع صورة منتج وصورة مشهد  
- يختار صورة او فيديو CGI
- يتابع التقدم في real-time
- يحمل النتيجة النهائية
- يشتري كريدت إضافي
- يشوف تاريخ مشاريعه

**كل ده يبقى شغال بسلاسة ومافيهوش اي مشاكل!**

**يلا ابدأ واعمل المعجزة دي! 🚀**